name: Build Zozety Love APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
    
    # 1. إنشاء هيكل Gradle إذا كان مفقودًا
    - name: Setup Gradle Wrapper
      run: |
        # أنشئ مجلد wrapper إذا لم يكن موجودًا
        mkdir -p gradle/wrapper
        # أنزل ملف gradle-wrapper.jar مباشرةً
        wget -q https://github.com/gradle/gradle/raw/rel/v8.5/wrapper/gradle-wrapper.jar -P gradle/wrapper/
      
    # 2. الآن gradlew يجب أن يعمل
    - name: Make gradlew executable
      run: |
        chmod +x ./gradlew || echo "gradlew not found, will download it"
        if [ ! -f "./gradlew" ]; then
          # أنزل سكريبت gradlew إذا كان مفقودًا
          wget -q https://raw.githubusercontent.com/gradle/gradle/master/gradlew
          chmod +x ./gradlew
        fi
        
    - name: Build with Gradle
      run: ./gradlew assembleDebug
      
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Zozety-Love-APK
        path: app/build/outputs/apk/debug/app-debug.apk
